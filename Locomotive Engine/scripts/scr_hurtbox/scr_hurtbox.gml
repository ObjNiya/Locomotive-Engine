function hurtboxInit()
{
    invincibleBuffer = 0;
}

function hurtboxStep(hurtbox_id)
{
    /*if (invincibleBuffer > 0)
        invincibleBuffer--;
    
    if (hurtbox_id == noone)
        exit;
    
    hurtbox_id.x = x + hurtbox_id.xOffset;
    hurtbox_id.y = y + hurtbox_id.yOffset;*/
}